MONGODB_URI=mongodb+srv://Cert-verifier:Anilkk123@cert-verifier.7shdmgf.mongodb.net/certdb?retryWrites=true&w=majority&appName=Cert-verifier

# --- Fabric toggle ---
USE_REAL_FABRIC=true
AS_LOCALHOST=true

# --- Fabric connection profile + wallet (relative to backend/) ---
FABRIC_CCP_PATH=../connection-org1.json
FABRIC_WALLET_PATH=./wallet

# --- Email (leave empty if not using email yet or put real Gmail creds) ---
EMAIL_USER=
EMAIL_PASS=